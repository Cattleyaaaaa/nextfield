# 画廊图片目录

每个子文件夹就是一本相册。例如把图片放进 `欧卡2/`，它们会出现在「欧卡2」相册。新建一个文件夹也会自动新增相册，不用修改代码。

支持 JPG、JPEG、PNG、WebP、AVIF 和 GIF。文件按名称从大到小排序；时间戳命名的照片会显示为「影像 01」等。可在相册文件夹内放 `_cover.jpg`（也支持其他图片格式）指定分类封面，它不计入相册图片。

直接放在本目录的图片会显示在「未分类」。本地开发时刷新页面即可查看；发布到线上需要重新构建和部署。
