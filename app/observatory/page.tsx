import type { Metadata } from "next";
export const metadata: Metadata = { title: "Performance Observatory", description: "NEXTFIELD 最近一次生产构建的体验预算快照。" };
const metrics = [["Static routes", "34", "Pages generated by the latest production build"], ["Home JS", "~192 kB", "First-load snapshot, including shared runtime"], ["Image policy", "Unoptimized", "Static-host compatible output"], ["Motion fallback", "Enabled", "prefers-reduced-motion and touch paths"], ["Audio", "Procedural", "No media download or autoplay"], ["External tracking", "None", "Local-first interactive state"]] as const;
export default function ObservatoryPage() { return <div className="mx-auto max-w-site px-5 pb-28 pt-20 sm:px-8 sm:pt-28 lg:px-12"><p className="text-xs font-semibold uppercase tracking-[.24em] text-accent">Performance observatory / 21</p><h1 className="mt-6 font-display text-[clamp(3rem,7vw,6rem)] leading-[.9] tracking-[-.06em]">THE EXPERIENCE<br />HAS A BUDGET.</h1><p className="mt-8 max-w-2xl text-lg leading-9 text-muted">这是最近一次生产构建的公开快照，不伪装成实时监控。每次架构或内容显著变化后更新。</p><div className="mt-14 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{metrics.map(([label, value, note]) => <article className="rounded-3xl border border-line bg-panel p-6" key={label}><p className="font-mono text-[9px] uppercase tracking-[.15em] text-accent">{label}</p><p className="mt-12 font-display text-5xl tracking-[-.06em]">{value}</p><p className="mt-3 text-xs leading-5 text-muted">{note}</p></article>)}</div><p className="mt-8 border-l-2 border-accent pl-4 text-xs leading-6 text-muted">预算目标：交互只增强理解；核心内容在 JavaScript、WebGL 或动画不可用时仍然可以访问。</p></div>; }
