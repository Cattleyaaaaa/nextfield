import type { Metadata } from "next";
import { FAILURE_ITEMS } from "@/lib/nextfield-data";

export const metadata: Metadata = { title: "失败博物馆", description: "没有进入最终版本的方案，以及它们留下的判断。" };

export default function FailuresPage() {
  return <div className="mx-auto max-w-site px-5 pb-28 pt-20 sm:px-8 sm:pt-28 lg:px-12"><p className="text-xs font-semibold uppercase tracking-[0.24em] text-accent">Failure museum / 04</p><h1 className="mt-6 max-w-5xl font-display text-[clamp(3rem,7vw,6rem)] leading-[0.9] tracking-[-0.065em]">THINGS THAT<br />DIDN&apos;T WORK.</h1><p className="mt-8 max-w-2xl text-lg leading-9 text-muted">这里不保存尴尬，而是保存判断形成的证据。每个被删除的方案，都应该至少留下一条可复用的经验。</p><div className="mt-16 space-y-5">{FAILURE_ITEMS.map((item) => <article className="overflow-hidden rounded-[1.75rem] border border-line bg-panel" key={item.number}><div className="grid md:grid-cols-[10rem_1fr]"><div className="border-b border-line p-6 md:border-b-0 md:border-r"><span className="font-display text-4xl text-accent">{item.number}</span><p className="mt-4 font-mono text-[9px] uppercase tracking-[0.15em] text-muted">{item.tag}</p></div><div className="p-6 sm:p-8"><h2 className="font-display text-3xl tracking-[-0.04em]">{item.title}</h2><dl className="mt-8 grid gap-6 lg:grid-cols-3"><div><dt className="font-mono text-[9px] uppercase tracking-[0.16em] text-accent">What I tried</dt><dd className="mt-3 text-sm leading-6 text-muted">{item.tried}</dd></div><div><dt className="font-mono text-[9px] uppercase tracking-[0.16em] text-accent">Why it failed</dt><dd className="mt-3 text-sm leading-6 text-muted">{item.failed}</dd></div><div><dt className="font-mono text-[9px] uppercase tracking-[0.16em] text-accent">What survived</dt><dd className="mt-3 text-sm leading-6 text-muted">{item.survived}</dd></div></dl></div></div></article>)}</div></div>;
}
