node_modules/
.next
.next-development
.next-production
.open-next/
.wrangler/
out
.vercel
*.log
.DS_Store
*.tsbuildinfo
.cache/*
!.cache/typescript/
!.cache/typescript/.gitkeep
tmp/
.env*
!.env.example

# Cloudflare local secrets and private credentials
.dev.vars*
!.dev.vars.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*credential*.json
*service-account*.json
*service_account*.json
*.p8
.npmrc
.netrc
.terraform/
*.tfstate
*.tfstate.*
secrets/
private/

# Local data, backups and test artifacts may contain user information
*.sqlite
*.sqlite3
*.db
*.db-journal
*.db-wal
*.db-shm
*.bak
*.backup
*.dump
coverage/
playwright-report/
test-results/

# Local editor/agent state; the curated .agents/skills assets remain tracked
.codex/
.workbuddy/
.claude/
.cursor/
.aider*

# Locally held radio tracks (kept out of Git; files remain available for local builds)
public/audio/

# Local source archive for UI experiments (exceeds GitHub's file size limit)
.agents/skills/react-bits-main.zip
