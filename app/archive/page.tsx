import type { Metadata } from "next";
import { ARCHIVE_MONTHS } from "@/lib/evidence-data";
export const metadata: Metadata = { title: "Field Archive", description: "按月份回看 NEXTFIELD 的内容与视觉变化。" };
export default function ArchivePage() { return <div className="mx-auto max-w-site px-5 pb-28 pt-20 sm:px-8 sm:pt-28 lg:px-12"><p className="text-xs font-semibold uppercase tracking-[0.24em] text-accent">Field archive / 19</p><h1 className="mt-6 font-display text-[clamp(3rem,7vw,6rem)] leading-[.9] tracking-[-.06em]">THE SITE<br />REMEMBERS.</h1><div className="mt-16">{ARCHIVE_MONTHS.map((item, index) => <article className="grid gap-6 border-t border-line py-10 lg:grid-cols-12" key={item.date}><div className="lg:col-span-3"><span className="font-mono text-xs text-accent">{item.date}</span></div><div className="lg:col-span-5"><h2 className="font-display text-3xl tracking-[-.04em]">{item.title}</h2><p className="mt-4 text-sm leading-7 text-muted">{item.note}</p></div><div className="lg:col-span-3"><ul className="space-y-2">{item.additions.map((entry) => <li className="text-xs text-muted" key={entry}>+ {entry}</li>)}</ul></div><div className="lg:col-span-1"><span className={`block size-12 rounded-full ${index === 0 ? "bg-liquid-foam" : index === 1 ? "bg-accent" : "bg-line"}`} title={item.tone} /></div></article>)}</div></div>; }
