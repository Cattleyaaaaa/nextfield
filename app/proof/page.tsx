import type { Metadata } from "next";
import { Download } from "lucide-react";
import { TransitionLink } from "@/components/site/transition-link";
import { PROOF_ROWS } from "@/lib/evidence-data";

export const metadata: Metadata = { title: "Proof Mode", description: "去除装饰后的职责、证据与结果清单。" };
export default function ProofPage() { return <div className="mx-auto max-w-site px-5 pb-28 pt-16 sm:px-8 lg:px-12"><div className="flex flex-wrap items-end justify-between gap-6 border-b-2 border-ink pb-8"><div><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-accent">Proof mode / no decoration</p><h1 className="mt-4 font-sans text-4xl font-semibold tracking-[-0.04em] sm:text-6xl">Agent + Full-stack evidence</h1></div><a className="inline-flex items-center gap-2 rounded-full bg-ink px-5 py-3 text-sm text-paper" download href="/downloads/NEXTFIELD-Field-Pack.pdf"><Download className="size-4" /> 下载 Field Pack</a></div><div className="overflow-x-auto"><table className="mt-8 w-full min-w-[760px] border-collapse text-left"><thead><tr className="border-b border-line font-mono text-[9px] uppercase tracking-[0.15em] text-muted"><th className="py-4 pr-5">Capability</th><th className="py-4 pr-5">Scope</th><th className="py-4 pr-5">Evidence</th><th className="py-4 pr-5">Outcome</th></tr></thead><tbody>{PROOF_ROWS.map((row) => <tr className="border-b border-line align-top" key={row.capability}><td className="py-6 pr-5 font-semibold">{row.capability}</td><td className="max-w-xs py-6 pr-5 text-sm leading-6 text-muted">{row.scope}</td><td className="max-w-xs py-6 pr-5"><TransitionLink className="text-sm underline decoration-line underline-offset-4 hover:text-accent" href={row.href}>{row.evidence}</TransitionLink></td><td className="max-w-xs py-6 text-sm leading-6">{row.result}</td></tr>)}</tbody></table></div><div className="mt-14 grid gap-6 border-y border-line py-8 sm:grid-cols-3"><div><p className="font-mono text-[9px] text-muted">FOCUS</p><p className="mt-2 text-sm">Agent products / AI applications</p></div><div><p className="font-mono text-[9px] text-muted">DELIVERY</p><p className="mt-2 text-sm">Product thinking to production UI</p></div><div><p className="font-mono text-[9px] text-muted">LOCATION</p><p className="mt-2 text-sm">NanChang / remote</p></div></div></div>; }
