# 更新记录

## V1.2.0 — 2026-09-26

- 新增 Cloudflare Analytics 统计看板：支持多时间范围、请求与访问指标、缓存率和带宽趋势、每日数据表及 JSON 导出。
- 在全站顶栏增加统计入口，并补充中文、英文界面与 sitemap 路由。
- 真实统计不可用时明确显示状态，不以示例数据冒充真实流量；示例模式需由访客主动选择。
- 稳定首页每日视觉种子的跨端计算，避免服务端与浏览器渲染值不一致。
- 更新首页介绍文案，强调项目、学习记录与持续探索。

真实 Cloudflare 数据需要部署环境配置具备目标 Zone Analytics 读取权限的服务端令牌。令牌和本地 `.env.local` 不包含在发布内容中。
